/node_modules
/dist
/bin/darwin-arm64
/bin/darwin-x64
/bin/win32-x64
/bin/yt-dlp
/main
/temp
/apps/app/dist
.DS_Store
/even-terminal-*.log
.claude/settings.local.json

# E2E: downloaded/derived media fixtures and per-run diagnostic artifacts.
# Both are large and reproducible — `npm run test:e2e:fixtures` rebuilds the
# first, and the second is only interesting while a failure is being chased.
/tests/e2e/.fixtures
/tests/e2e/.out
dist-web/
